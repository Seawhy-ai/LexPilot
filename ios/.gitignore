# Xcode / xcodebuild build artifacts
build
DerivedData

# CocoaPods (Capacitor does not use them, but be safe)
Pods

# Web assets — regenerated by `npx cap sync ios`
public
ios/App/App/public

# User-specific Xcode state
xcuserdata

# Cordova plugin bridge generated by cap sync
capacitor-cordova-ios-plugins

# Generated config files (recreated on every sync, relative to this repo)
ios/App/App/capacitor.config.json
ios/App/App/config.xml
